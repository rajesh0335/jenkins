pipeline {
	agent any
	stages {
		stage ('build') {
		echo "this is build"
		}
		stage ('test: integration-&-quality') {
		echo "this is test"
		}
		stage ('test: functional') {
		echo "this is build"
		}
		stage ('test: load-&-security') {
		echo "this is build"
		}
		stage ('approval') {
		}
		stage ('deploy:prod') {
		}
	}
}

